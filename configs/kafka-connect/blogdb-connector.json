{ 
	"name": "blogdb-connector", 
	"config": { 
		"connector.class": "io.debezium.connector.mysql.MySqlConnector", 
		"tasks.max": "1", 
		"database.hostname": "blogdb", 
		"database.port": "3306", 
		"database.user": "spring", 
		"database.password": "spring", 
		"database.server.id": "3000", 
		"database.server.name": "blogdb", 
		"database.whitelist": "blogpost", 
		"database.history.kafka.bootstrap.servers": "broker:9092", 
		"database.history.kafka.topic": "dbhistory.blogdb" 
	}
}
